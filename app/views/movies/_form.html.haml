= simple_form_for @movie, wrapper: :horizontal_form, html: { class: 'form-horizontal' } do |f|
  = f.input :title
  = f.input :imdb_id
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.button :submit, class: 'btn btn-primary', data: { :'disable-with' => '...' }
